{
  "name": "welcome",
  "version": "1.0.0",
  "description": "A simple web page project",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build:starcastle": "cd StarCastle/2d && npm install && npm run build && cp -r ../dist/. public/img public/sound ../../public/starcastle/",
    "dev": "mkdir -p public/starcastle && npm run build:starcastle && vite"
  },
  "dependencies": {
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^8.0.7"
  }
}
